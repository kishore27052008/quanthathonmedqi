Drop the trained model here as:
  - model.pkl      (joblib-dumped XGBoost/RandomForest pipeline with predict_proba)
  - features.json  (ordered list of feature names the model expects)

Domain: gestational_diabetes
